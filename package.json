{
  "name": "dynamodb-hono-notes-api",
  "version": "1.0.0",
  "description": "Note taking API with Hono and DynamoDB",
  "main": "src/index.ts",
  "scripts": {
    "dev": "tsx watch src/index.ts"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.0.0",
    "@aws-sdk/lib-dynamodb": "^3.0.0",
    "@hono/node-server": "^1.0.0",
    "dotenv": "^16.0.0",
    "hono": "^3.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  }
}